# Implementation: <task-name>

> **Maintenance rule for Claude:** this file is the impl-stage work log. Update it incrementally **as work happens** — do NOT leave it as a template. Concretely:
> - When you finish a milestone or a self-contained chunk → append a line to `## What was built` describing it (what now works, in user-facing terms).
> - When you create or substantially modify a file → append its path to `## Files changed`.
> - When you make a decision a reviewer would want to know about (workaround, non-obvious choice, deferred concern) → add it to `## Notes for review`.
>
> If you implement two milestones without touching this file, you are doing it wrong. Stop and update it first.

## What was built

<cumulative: what's been done — append as you go>

## Files changed

<main paths affected — append as you touch them>

## Notes for review

<what a reviewer should pay attention to — append when relevant>
